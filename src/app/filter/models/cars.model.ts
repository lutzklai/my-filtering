export interface GetCarAPI {
    acc: number; // 0-60 mph time (seconds)
    cyl: number; // cylinders
    dsp: number;
    hp: number; // horsepower
    lbs: number; // weight (lbs)
    mpg: number; // miles per gallon
    name: string;
    origin: number; // ?
    year: number; // year
};